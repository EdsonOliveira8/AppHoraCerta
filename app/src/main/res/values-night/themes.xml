<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.MyApplication" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Modo escuro -->
        <item name="colorPrimary">@color/Azul</item>
        <item name="colorOnPrimary">@color/buttonText</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColor">@color/white</item>
        <item name="buttonStyle">@style/CustomButton</item>
    </style>

        <style name="CustomButton" parent="Widget.AppCompat.Button"><item name="android:background">@drawable/button_background</item>
        <item name="android:backgroundTint">@color/Azul</item>
        <item name="android:textColor">@color/buttonText</item>
    </style>
</resources>
